{{#each books}}


{{> partial}}

    <h1> <small>Title: {{title}}</small></h1>
    <!-- <div> -->
      <!-- {{#if desc}}
        {{desc}}
      {{else}}
        No author.
      {{/if}} -->
    <!-- </div> -->

  {{/each}}
